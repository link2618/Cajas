@model IEnumerable<cajasqlserver.Models.t_tipo_caja>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idtipocaja">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idtipocaja">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idtipocaja">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
